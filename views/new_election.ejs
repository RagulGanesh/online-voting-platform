<!DOCTYPE html>
<html lang="en">
<head>
    <!-- new election page -->
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
    <link rel="icon" type="image/x-icon" href="/img/vote.png">
    <style>
        body{
          background-size:cover;
        }
        #roundButton{
            border-radius: 50px;
        }
        .four{
            display: flex;
            flex-direction: row;
        }
        .five{
            display: flex;
            flex-direction: row;
        } 
        .six{
            background-color: rgb(106, 104, 104);
            border: 2px solid aqua;
        }
      </style>
</head>
<body class="bg-gray-50" background="img/image.jpg">
    <%- include('header.ejs') %>
    <!-- new election page -->
    <div class="flex gap-1 mx-10 mt-6 bg-green-200">
        <span>--></span>
        <a href="/elections" class="text-cyan-800 hover:text-cyan-600 font-serif">Here are all the elections!!!</a>
        <span>--></span>
    </div>
    <div class=" flex text-gray-800 antialiased flex-col overflow-hidden bg-yellow-200 py-6 sm:py-12">
        <div class=" py-3 sm:w-96 mx-auto text-center">
            <span class="text-2xl font-light font-serif text-decoration-line: underline text-red-400">Create an election</span>
            <div>
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-cyan-100 my-4 list-none rounded-md px-4 py-2 font-serif"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
            <!-- new election page -->
            <div class="mt-4 bg-white shadow-md rounded-lg text-left">
                <div class="h-2 bg-green-400 rounded-t-md"></div>
                <form action="/elections" method="post" class="px-10 py-8">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                    <label for="electionName" class="block mt-3 font-semibold font-serif text-red-400">What is your election's name?</label>
                    <input id="electionName" name="electionName" type="text" placeholder="Election name" class="six border w-full h-5 px-3 py-7 mt-2 hover:outline-none focus:outline-none focus:ring-indigo-500 focus:ring-1 rounded-md font-serif" required>
                    <label for="url" class="block mt-3 font-semibold font-serif text-red-400">Enter your custom URL string (With no spaces)</label>
                    <input id="url" name="url" type="text" placeholder="Election URL String" class="six border w-full h-5 px-3 py-7 mt-2 hover:outline-none focus:outline-none focus:ring-indigo-500 focus:ring-1 rounded-md font-serif" required>
                    <button type="submit" id="roundButton" class=" mt-6 w-full bg-cyan-500 text-white py-2 px-6 rounded-md hover:bg-cyan-600 font-serif hover:text-indigo-900">Create</button>
                </form>
                <div class="h-2 bg-green-400 rounded-t-md"></div>
            </div>
        </div>
    </div>
</body>
</html>
<!-- new election page -->
<!-- new election page -->