<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>New question</title>
</head>
<body class="bg-gray-50">
    <%- include('header.ejs') %>
    <div class="flex gap-1 mx-10 mt-6 bg-gray-50">
        <span>></span>
        <a href="/elections" class="text-cyan-800 hover:text-cyan-600 font-serif">All Elections</a>
        <span>></span>
        <a href="/elections/<%= id %>/" class="text-cyan-800 hover:text-cyan-600 font-serif">Election</a>
        <span>></span>
        <a href="/elections/<%= id %>/questions" class="text-cyan-800 hover:text-cyan-600 font-serif">Questions</a>
    </div>
    <div class=" flex text-gray-800 antialiased flex-col  overflow-hidden bg-gray-50 py-6 sm:py-12">
        <div class=" py-3 sm:w-96 mx-auto text-center">
            <span class="text-2xl font-light font-serif">Add a new Question</span>
            <div>
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-cyan-100 my-3 list-none rounded-md px-4 py-2 font-serif"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
            <div class="mt-4 bg-white shadow-md rounded-lg text-left">
                <div class="h-2 bg-cyan-400 rounded-t-md"></div>
                <form action="/elections/<%= id %>/questions/create" method="post" class="px-8 py-6">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                    <label for="questionName" class="block mt-3 font-semibold font-serif">What is the question?</label>
                    <input id="questionName" name="questionName" type="text" placeholder="Question" class="border w-full h-5 px-3 py-5 mt-2 hover:outline-none focus:outline-none focus:ring-indigo-500 focus:ring-1 rounded-md font-serif" required>
                    <label for="description" class="block mt-3 font-semibold font-serif">Description</label>
                    <textarea id="description" name="description" class="border w-full px-2 py-1 mt-2 hover:outline-none focus:outline-none focus:ring-indigo-500 focus:ring-1 rounded-md font-serif" placeholder="Description" rows="4" cols="20"></textarea>
                    <button type="submit" class="mt-6 w-full bg-cyan-500 text-white py-2 px-6 rounded-md hover:bg-cyan-600 font-serif">Add question</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>